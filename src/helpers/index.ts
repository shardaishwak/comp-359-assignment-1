import generateRandomArray from "./generateRandomArray";
import sleep from "./sleep";

export default class Helpers {
	public static generateRandomArray = generateRandomArray;
	public static sleep = sleep;
}
